[project]
name = "dharma-core"
version = "1.0.0"
description = "Reference implementation of the Dharma Protocol"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
